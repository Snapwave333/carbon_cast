---
type: fix
area: ui
---

The playlist sections in the left rail (All channels, Groups, Movies, Series …)
now show their name under the icon, so they are no longer a row of unlabelled
grey squares that is easy to miss. The rail also keeps the settings button
pinned in view and only scrolls the destinations above it, instead of pushing
the bottom entries off-screen on short windows.
