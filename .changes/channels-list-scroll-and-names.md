---
type: fix
area: channels
---

The channel list scrollbar now matches the real list length, so the end of a
large playlist is reachable again instead of overshooting. Channel names that
happened to match an internal label no longer render as that label, the
playing channel and the favourite toggle announce their state to screen
readers, and logos load lazily while scrolling.
