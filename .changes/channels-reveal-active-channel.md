---
type: fix
area: channels
---

The channel list now scrolls the playing channel into view when it is set from
outside the list — resuming your last channel, opening a global search result,
or entering a channel number. Previously the row was highlighted but could sit
thousands of rows off-screen. A channel that is already visible is left where
it is.
