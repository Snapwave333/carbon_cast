---
type: fix
area: branding
---

CarbonCast IPTV now uses the same Signal Blade mark across Windows, macOS,
Linux, browser favicons, and PWA icons. The complete icon set is generated from
`app-icon.svg`, including native favicon sizes and mask-safe Android artwork,
so every install and browser surface stays visually consistent.
