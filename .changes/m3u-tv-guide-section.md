---
type: feature
area: playlists
---

TV Guide is now a full-width playlist tab and the default landing view. It
keeps the active channel playing, resumes the last channel, handles overnight
programmes correctly, updates the live clock, supports keyboard programme
access, ignores stale searches, and offers Today and retry controls.
