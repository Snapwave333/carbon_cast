---
type: feature
area: ui
---

CarbonCast now opens on a proper splash: the monogram draws itself onto the
carbon weave with a red glow, the wordmark rises beneath it, and a sweep bar
runs while the app loads — then it fades away over the ready interface instead
of cutting. It respects the system "reduce motion" setting.
