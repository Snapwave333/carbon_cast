---
type: feature
area: docker
---

Self-hosted images are now published to the GitHub Container Registry. Pull
`ghcr.io/snapwave333/carbon_cast:latest` — or a `<version>` tag — instead of
building the image yourself.
