---
type: feature
area: ui
---

Every icon in the app is now a custom hand-drawn SVG set with a consistent
thin-stroke look, replacing the Material Icons font. Icons render sharper at
every size, and dropping the bundled font makes the app smaller and faster to
load.
