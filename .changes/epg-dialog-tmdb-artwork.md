---
type: feature
area: epg
---

Programme details in the TV guide now show artwork even when your EPG feed
has none: with TMDB metadata enabled in Settings, the dialog looks the
programme up by title (film or series, guessed from its genre) and uses the
TMDB backdrop as its hero image. Lookups are cached and best-effort — no
confident match keeps the current look.
