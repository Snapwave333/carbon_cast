---
type: fix
area: playback
---

The playback bar's medium and large sizes now lay the player out as a stage —
centred artwork, larger title, roomier controls — instead of stretching the
compact strip. Its buttons are translated again rather than showing raw labels,
and the popped-out window keeps the app's colours. Each visualizer blob now has
its own colour, and the beat swells instead of snapping.
