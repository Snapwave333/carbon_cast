---
type: feature
area: settings
---
Settings now has a Network section that routes the built-in players through a SOCKS5 or HTTP proxy, so region-locked channels can play from another country. Only CarbonCast IPTV's own streaming is affected — the rest of your system keeps its normal connection. A Test connection button reports the exit IP and country before you save, and changes apply without restarting.
