/**
 * Buy Me a Coffee page for the project, e.g.
 * 'https://www.buymeacoffee.com/<your-slug>'.
 *
 * Every coffee button on the site is gated on this value: while it is null
 * nothing renders, so the site never ships a donation link pointing at an
 * account that isn't yours. Set it once here to switch them all on.
 */
export const BUY_ME_A_COFFEE_URL: string | null = null;
